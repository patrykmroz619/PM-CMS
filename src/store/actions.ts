import userSlice from "./slices/userSlice";
import projectsSlice from "./slices/projectsSlice";

const userActions = userSlice.actions;
const projectsActions = projectsSlice.actions;

export { userActions, projectsActions };
